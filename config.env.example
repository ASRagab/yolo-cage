# yolo-cage configuration
# Copy to ~/.yolo-cage/config.env and edit with your credentials

# Required: GitHub Personal Access Token
# Generate at: https://github.com/settings/tokens
# Required scopes: repo (full control of private repositories)
GITHUB_PAT=ghp_your_token_here

# Required: Repository URL to work on
REPO_URL=https://github.com/your-org/your-repo.git

# Optional: Git author identity (defaults shown)
GIT_NAME=yolo-cage
GIT_EMAIL=yolo-cage@localhost

# Optional: Claude OAuth credentials for Claude Code authentication
# Leave commented to use device login flow
# To get this value, copy the contents of ~/.claude/.credentials.json (single line)
# IMPORTANT: Wrap the JSON in single quotes to preserve the double quotes inside
# Example: CLAUDE_OAUTH='{"claudeAiOauth":{"accessToken":"sk-ant-oat01-...","refreshToken":"...","expiresAt":...}}'
# CLAUDE_OAUTH=

# Optional: Additional domains to bypass the egress proxy
# Comma-separated list (api.anthropic.com is always bypassed)
# PROXY_BYPASS=example.com,internal.corp

# Optional: Pod resource limits and requests (defaults shown)
# POD_MEMORY_LIMIT=4Gi
# POD_MEMORY_REQUEST=1Gi
# POD_CPU_LIMIT=4
# POD_CPU_REQUEST=500m
